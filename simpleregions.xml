<Layer LoadCommand = "%xero(function(self)
    function region(t)
        local pn = t.plr or get_plr()
        if type(pn) == 'number' then pn = {pn} end
        for _, plr in ipairs(plr) do
            if FUCK_EXE then
                P[pn]:SetHiddenRegions({{t[1], t[1] + t[2]}})
            else
                P[pn]:GetChild('NoteField'):add_hidden_regions({{t[1], t[1] + t[2]}})
            end
        end
        return region
    end
end)" />